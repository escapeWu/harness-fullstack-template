# This is NOT the Next.js you know

This project uses a minimal frontend scaffold. Before making substantial frontend changes, read the local framework docs for the installed version and align with actual project dependencies.

Keep HTTP calls centralized in `src/lib/api.ts`.
